# Neon (injetado automaticamente pela integração Vercel -> Storage -> Neon)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
DIRECT_URL="postgresql://user:pass@host/db?sslmode=require"

# Protege a rota /api/cron/dri (Vercel Cron envia este header)
CRON_SECRET="troque-por-um-valor-aleatorio"

# Auth — Semana 1. Sem estas vars o app roda em modo dev (respondente por cookie).
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
# CLERK_SECRET_KEY=""
