node_modules
.next
.env
.env*.local
*.tsbuildinfo
next-env.d.ts

# Export de cadastros (contém hashes de senha) — nunca versionar
cadastros-export.json

# Dado real de cliente — nunca versionar
examples/CONTROLE MASTER MRS.xlsx
